[package]
name = "alexa_cgi_core"
version = "0.1.0"
edition = "2021"
authors = ["schettj"]
description = "A lightweight, zero-dependency CGI framework for self-hosted Amazon Alexa skills in Rust."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
